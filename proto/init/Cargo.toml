[package]
name = "proto-init"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
repository.workspace = true
license = "MIT"
description = "The protocol of init: its method numbers and the start data of a program"

[lib]
bench = false

[dependencies]
abi = { path = "../../lib/abi" }
proto-wire = { path = "../wire" }
