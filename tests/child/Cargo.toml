[package]
name = "test-child"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
repository.workspace = true
license = "GPL-3.0-or-later"
description = "The child program of the test init: one program whose role its parent names at its start"

# What the test init and the program agree on.
[lib]
name = "child"
test = false
bench = false

[[bin]]
name = "test-child"
test = false
bench = false

[dependencies]
abi = { path = "../../lib/abi" }
bootimg = { path = "../../lib/bootimg" }
rt = { path = "../../lib/rt" }
